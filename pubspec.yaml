name: suraksha_setu
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.7.2
dependencies:
  url_launcher: ^6.2.5
  geolocator: ^10.0.0
  geocoding: ^3.0.0
  http: ^1.2.0
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.3
  equatable: ^2.0.5
  video_player: ^2.7.0
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
flutter:
  assets:
    - assets/logo.png
    - assets/LOGO (1).png
    - assets/videos/earthquake.mp4
    - assets/videos/bomb_public.mp4
  uses-material-design: true